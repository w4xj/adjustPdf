# 版本记录

## Unreleased

### 工程化

- 统一 `pyproject.toml`、`src/adjust_pdf/__init__.py`、README 和 Windows 版本资源中的版本号；
- 将运行时和开发依赖集中到 `pyproject.toml`，保留 requirements 文件作为 pip 兼容入口；
- 增加 Ruff、pytest-cov、pre-commit 和 nox 配置；
- 增加 GitHub Actions 测试、覆盖率和 Windows EXE 构建流程；
- 增加贡献指南、行为准则、Issue 模板和 Pull Request 模板；
- 将截图目录加入忽略规则，避免本地截图污染仓库。

## 0.5.3 - 2026-07-28

### 新增

- 新增“检查页面属性”独立按钮，逐页显示宽（Width）、高（Height）、旋转（Rotate），明确标注尺寸方向；
- 解析 PKCS7/CMS SignedData，提取证书信息（签名人、颁发机构、序列号、有效期）；
- 签名报告按签署事件分组展示，同一事件多个签名字段合并显示；
- 增加国密 SM2/SM3 证书支持（四川 CA 等），无需外部密码学库；
- 新增 `signature_parser.py` 模块和 ASN.1 DER 解析逻辑；
- 新增 `asn1crypto` 依赖。

### 修复

- 移除基于 `/V` 间接引用 idnum 的去重逻辑，修正电子印章多字段场景的漏报；
- 修复 `2081980609123209218.pdf` 之前只报告 3 个签名、现在正确报告 9 个签名字段的问题。

## 0.5.2 - 2026-07-24

### 改进

- 文件列表表头使用与“添加 PDF”同系的淡黄色背景和白色字体；
- 将卡片、表格、输入框和按钮改为高透明玻璃风格；
- 降低背景压暗层，优先展示 LTY 原图细节；
- 保留暖金色边框和加粗浅色文字，确保功能文字可读；
- 将红色签章警示改为半透明红底和清晰边框；
- 更新皮肤验收截图与相关说明。

## 0.5.1 - 2026-07-24

### 改进

- 将 GUI 从 Tkinter 重构为 PySide6；
- 使用 Qt RGBA 样式实现半透明悬浮卡片；
- 重构为标题、文件、处理选项和执行状态四张独立卡片；
- 持续显示 LTY 全局背景；
- 将电子签章重要提示改为深红色高对比警示条；
- 增加 Qt 离屏渲染和正式 EXE 实际启动截图验收；
- 扩充皮肤文档。

## 0.5.0 - 2026-07-23

### 新增

- 使用 `assets/lty.jpg` 增加 LTY 背景皮肤；
- 增加全窗口背景画布和居中裁剪效果；
- 增加深色内容面板、金色按钮和统一表格配色；
- 窗口缩放时背景和内容区域自动适配；
- 构建脚本自动生成并打包皮肤资源。

## 0.4.0 - 2026-07-23

### 新增

- 增加已签署 PDF 的标准数字签名结构检测；
- 主界面增加已签章风险常驻提示；
- 点击“开始处理”前自动执行签名预检；
- 检测到数字签名时显示二次确认窗口，默认取消处理；
- 命令行模式默认拒绝处理已签名 PDF，确认风险后可使用 `--allow-signed`；
- 增加真实签章 PDF 检测和安全策略测试。

## 0.3.1 - 2026-07-23

### 变更

- 使用 `assets/icon/tool box-512x512.png` 生成多尺寸 Windows ICO 图标；
- EXE 和 GUI 使用统一应用图标；
- 构建脚本自动携带图标资源。

## 0.3.0 - 2026-07-23

### 新增

- 增加“检查页面旋转”按钮；
- 支持在处理前列出所有有效旋转页面；
- 增加多文件检查、失败提示和可滚动报告窗口；
- 增加旋转检查报告格式化及自动化测试。

## 0.2.0 - 2026-07-23

### 新增

- GUI 默认选中“仅处理第一页”；
- 增加“按页码处理”模式；
- 支持 `1,5-9,12` 页码列表和范围语法；
- GUI 和 CLI 共用页码解析与校验。

## 0.1.0 - 2026-07-22

### 新增

- pypdf 页面旋转固化引擎；
- 支持 `Rotate 90/180/270` 归零；
- 支持所有旋转页、仅第一页和指定页处理模式；
- Tkinter 图形界面、中文路径和安全输出命名；
- 批注、链接和表单区域警告；
- 命令行检查与处理模式；
- 自动化测试、PyInstaller 构建和 EXE 冒烟测试脚本；
- 用户、开发、架构、发布、故障排查和 PDF 结构文档；
- PDF 结构检查脚本 `scripts/inspect_pdf.py`。
